{
    "name": "splicewire/laravel-beam-media",
    "description": "The media arm of the beam family: the generic UUID-native Media model (extends spatie/laravel-medialibrary's base Media with a uuid primary key), the declarative primary-image traits (HasPrimaryImages / HasFeaturedImage), and the ubiquitous media table migrations (central + tenant). Extracted from beam-core (HTTP-03 / ADR-0178): beam-media owns spatie/laravel-medialibrary and requires it DOWN; base beam sheds the medialibrary dependency and does NOT require beam-media, and beam-media does NOT require base beam — no cycle. A host (Tower) subclasses Models\\Media and binds it via config('beam-media.model').",
    "keywords": ["laravel", "schemastud", "beam", "media", "medialibrary", "spatie"],
    "license": "MIT",
    "authors": [{ "name": "Stephen Rushing", "email": "stephen@rushing.dev" }],
    "require": {
        "php": "^8.2",
        "illuminate/contracts": "^11.0|^12.0|^13.0",
        "illuminate/support": "^11.0|^12.0|^13.0",
        "spatie/laravel-data": "^4.0|^5.0",
        "spatie/laravel-medialibrary": "^11.0",
        "splicewire/laravel-beam": "*"
    },
    "require-dev": {
        "orchestra/testbench": "^9.0|^10.0|^11.0",
        "phpunit/phpunit": "^11.0|^12.0",
        "laravel/pint": "^1.0"
    },
    "autoload": { "psr-4": { "Splicewire\\Beam\\Media\\": "src/" } },
    "autoload-dev": { "psr-4": { "Splicewire\\Beam\\Media\\Tests\\": "tests/" } },
    "extra": {
        "branch-alias": { "dev-main": "1.0.x-dev" },
        "laravel": { "providers": ["Splicewire\\Beam\\Media\\BeamMediaServiceProvider"] },
        "package-topology": {
            "because": "beam-media owns medialibrary (DOWN) and — as a particle CONSUMER (HTTP-10: the MediaData #[ParticleResource] + the download/ingest #[ParticleOp]s) — depends DOWN on base beam for the particle machinery, exactly as the sibling arms (accounts/bookmarks/taxonomy) do. The ADR-0178 invariant is one-directional: base beam does NOT require beam-media (no reverse edge → no cycle); beam-media → beam is the natural media-arm direction.",
            "downOnly": ["spatie/laravel-medialibrary", "splicewire/laravel-beam"]
        }
    },
    "repositories": [
        { "type": "path", "url": "../*", "options": { "symlink": true } },
        { "type": "path", "url": "../../rushing/*", "options": { "symlink": true } },
        { "type": "path", "url": "../../schemastud/*", "options": { "symlink": true } }
    ],
    "minimum-stability": "dev",
    "prefer-stable": true,
    "scripts": { "test": "@php vendor/bin/phpunit", "pint": "@php -d memory_limit=512M vendor/bin/pint" },
    "config": {
        "sort-packages": true,
        "allow-plugins": { "pestphp/pest-plugin": true, "php-http/discovery": true }
    }
}
